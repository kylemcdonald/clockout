{
  "name": "toggl",
  "version": "1.0.0",
  "description": "",
  "main": "app.mjs",
  "scripts": {
    "start": "node app.mjs",
    "deploy": "git push && ssh clockout \"source ~/.nvm/nvm.sh && cd ~/clockout && git pull && npm install --silent && pm2 restart clockout --silent\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "ws": "^8.19.0"
  }
}
